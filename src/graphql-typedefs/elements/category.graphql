type Category {
    id: ID
    name: String!
    icon: String!
    parentCategoryId: ID
    parentCategory: Category
    childrenCategories: [Category!]
    childrenElementGroups: [ElementGroup!]
}

type Query {
    category(id: ID!): Category
    categories(parentCategoryId: ID): [Category!]!
}