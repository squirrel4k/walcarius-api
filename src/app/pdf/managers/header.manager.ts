import { Injectable } from "@nestjs/common";

@Injectable()
export class HeaderManager {
    /* NOTE :
     * ALL header methods referenced in "PDF_HEADER" enum MUST ask for config & data as parameters.
     * If not, unexpected behavior will occur.
     */

}